

services:
  price-scraper:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crypto-price-scraper
    env_file:
      - .env
    environment:
      - TZ=UTC
    # Mount volume for log persistence
    volumes:
      - ./logs:/app/logs
    restart: "no"
