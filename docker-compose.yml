

services:
  price-scraper:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crypto-price-scraper
    environment:
      - TZ=UTC
    # Uncomment the following to mount a volume for data persistence
    # volumes:
    #   - ./data:/app/data
    restart: "no"
